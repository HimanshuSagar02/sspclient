# Local development - Vite proxy handles /api requests to localhost:5000
# No VITE_API_URL needed locally (defaults to /api which is proxied)

# For production build, set this in Render env vars:
# VITE_API_URL=https://sspbackend-um4y.onrender.com/api
